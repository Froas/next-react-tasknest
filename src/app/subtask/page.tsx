"use client";

import { withAuth } from '../../hoc/withAuth'
const Subtask = () => {
    return (
        <>
            <div>
                Subtask page
            </div>
        </>
    )
}

export default withAuth(Subtask);
