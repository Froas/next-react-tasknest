"use client";

import { withAuth } from '../../hoc/withAuth'
const Goal = () => {
    return (
        <>
            <div>
                Goal page
            </div>
        </>
    )
}

export default withAuth(Goal);
